
<div id="show-container" class="container">

  <div class="profile-pic mx-auto">
    <br>
  <%= cl_image_tag @user.photo.key, class: "avatar-xl mb-5 justify-content-center" %>
  <h3><%= @user.first_name.capitalize  %> <%= @user.last_name.capitalize  %></h3>
  <br>
  </div>
  <br>
  <h2 class="text-left">Mes Jeux</h2>
  <%= render 'shared/game_card', user_games: @user_games %>
  <% if @user == current_user %>
    <p>bouton vers "éditer mon profile"</p>
  <% end %>

  <h2> Mes Comptes </h2>
  <%= render 'shared/platform_card' %>

</div>
