<!DOCTYPE html>
<html>
  <head>
    <title>My Games - Le réseau social des gamers !</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>

    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload', defer: true %>
  </head>

  <body>
    <%= render 'shared/flashes' %>
    <div class="d-flex">
      <div><%= render 'shared/navbar' unless devise_controller? %></div>
      <div><%= render 'shared/sidebar' unless devise_controller? %></div>
      <div><%= yield %></div>
      <div><%= render 'shared/search' unless devise_controller? %></div>
    </div>
  </body>
</html>
